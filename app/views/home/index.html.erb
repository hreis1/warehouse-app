<h1>Galpões & Estoque</h1>

<h2>Galpões</h2>
<% @warehouses.each do |w| %>
  <p><%= link_to w.name, warehouse_path(w) %></p>
  <p>Código: <%= w.code %></p>
  <p>Cidade: <%= w.city %></p>
  <p><%= w.area %> m²</p>
<% end %>

<% if @warehouses.empty? %>
  <p>Nenhum galpão cadastrado</p>
<% end %>